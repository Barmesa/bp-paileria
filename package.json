{
  "name": "bp-paileria",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.22",
    "vitest": "^2.1.3",
    "wrangler": "^3.83.0"
  },
  "dependencies": {
    "papaparse": "^5.4.1"
  }
}
