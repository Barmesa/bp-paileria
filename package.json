{
  "name": "bp-paileria",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.59",
    "vitest": "^3.2.3",
    "wrangler": "^4.22.0"
  },
  "dependencies": {
    "papaparse": "^5.5.3"
  }
}
