{
  "name": "bp-paileria",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.7.3",
    "vitest": "^3.0.7",
    "wrangler": "^3.110.0"
  },
  "dependencies": {
    "papaparse": "^5.5.2"
  }
}
