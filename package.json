{
  "name": "bp-paileria",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.6.16",
    "vitest": "^3.0.6",
    "wrangler": "^3.109.3"
  },
  "dependencies": {
    "papaparse": "^5.5.2"
  }
}
